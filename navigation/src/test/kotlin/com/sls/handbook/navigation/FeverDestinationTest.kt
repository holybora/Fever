package com.sls.handbook.navigation

import org.junit.Assert.assertNotNull
import org.junit.Test

class FeverDestinationTest {

    @Test
    fun `FeverDestination is instantiable`() {
        assertNotNull(FeverDestination)
    }
}
